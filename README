## Vorlage verwenden
Zur Kompilierung entweder das Makefile mittels 'make' verwenden, oder folgende Befehle ausführen:

    $ pdflatex dokumentation
    $ biber dokumentation
    $ makeglossaries dokumentation
    $ pdflatex dokumentation

## Nur Deckblatt verwenden
    \usepackage{pdfpages}
    % .....
    \includepdf[pages=1]{../latexVorlage/output/dokumentation.pdf}

## Benutzung mit makefile
	$ make all			% Generiert pdf in Outputdirectory und loescht temporaere Files
	$ make clean		% loescht output
